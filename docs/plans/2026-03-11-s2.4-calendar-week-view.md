# S2.4 — Calendar: Add Week View

**Date:** 2026-03-11
**Sprint:** 2 (Readiness Audit)

## Problem

Calendar module has month + agenda views but no week view. The audit flagged this as a gap.

## Current State

- `/calendar/month/page.tsx` — FullCalendarView (month grid)
- `/calendar/agenda/page.tsx` — AgendaList (chronological)
- `calendar.store.ts` — fetchEvents, view state, source filter
- `mock-calendar.ts` — 15 events from vault dates + task due dates
- No `/calendar/week` page or WeekCalendarView component

## Plan

### Task 1: Create CalendarWeekView organism

**File:** `apps/web/src/components/organisms/CalendarWeekView.tsx`

- 7-column grid (Mon–Sun) showing current week
- Hour rows (8am–8pm) with event blocks positioned by time
- All-day events in top row
- Events color-coded by source (contracts=red, crm=blue, tasks=amber, calendar=green)
- Click event to open CalendarEventModal
- Uses calendar.store for events and navigation

### Task 2: Create /calendar/week page

**File:** `apps/web/src/app/(shell)/(modules)/calendar/week/page.tsx`

- Import CalendarWeekView
- Filter events by source using FilterPills (same as agenda page)
- Week navigation (prev/next week buttons)

### Task 3: Update calendar layout navigation

**File:** `apps/web/src/app/(shell)/(modules)/calendar/layout.tsx` (modify if exists, or page.tsx)

- Ensure the view switcher tabs include Week between Month and Agenda
- Wire navigation to /calendar/week

## Files to Create

- `apps/web/src/components/organisms/CalendarWeekView.tsx`
- `apps/web/src/app/(shell)/(modules)/calendar/week/page.tsx`

## Files to Modify

- Calendar layout/navigation (if applicable)

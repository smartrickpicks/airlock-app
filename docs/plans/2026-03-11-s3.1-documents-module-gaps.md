# S3.1 — Documents Module: Gap Assessment

**Date:** 2026-03-11
**Sprint:** 3

## Status: Already Built (~90%)

Exists: library page, DocumentsTable, DocumentPreview, DocumentUploadModal, FileDropZone, NewDocumentEditor, SpreadsheetView, documents.store, mock-documents, API routes (upload, list, get, content).

## Remaining Gap

The library page works but there's no way to trigger document upload from the UI. The DocumentUploadModal exists as a component but may not be wired into the library page.

### Task 1: Verify upload modal is accessible from library page

Read `apps/web/src/app/(shell)/(modules)/documents/library/page.tsx` and check if DocumentUploadModal is rendered and triggerable. If not, add an "Upload" button in the toolbar that opens it.

### Task 2: Verify file drop zone is on the library page

Check if FileDropZone is integrated. If not, add it as a drop target on the library page.

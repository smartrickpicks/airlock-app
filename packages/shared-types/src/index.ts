/**
 * @airlock/shared-types
 *
 * Generated TypeScript types from the Airlock API OpenAPI spec.
 * Run `pnpm generate-types` to regenerate after API changes.
 *
 * Pipeline: Pydantic models → FastAPI OpenAPI → hey-api/openapi-ts → this package
 */

// Placeholder types until OpenAPI codegen is configured
export interface HealthResponse {
  status: string;
  service: string;
}

// Re-export generated types here once available:
// export * from "./generated";
